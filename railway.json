{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip3 install -r requirements.txt && python3 manage.py collectstatic --noinput"
  },
  "deploy": {
    "startCommand": "python3 manage.py migrate && gunicorn gestion_pieces.wsgi:application",
    "restartPolicyType": "ON_FAILURE"
  }
}